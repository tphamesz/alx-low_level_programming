#include "main.h"
/**
*_islower - check main
*@c: An input character
*Description: function uses _putchar function to print
*alphabet in lowercase 10 times
*Return: 1 if it is lowercase and 0 if it  is uppercase
*/
int _islower(int c)
{
char i;
int lower = 0;
{
if (i == c)
lowe r = 1;
}
return (lower);
}
}
